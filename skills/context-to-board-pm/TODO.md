# context-to-board-pm — tracked items

- **Pre-publish trim (before pushing to Hololoop-AI):** remove the v1 meta-narrative — the
  "why v1 grew a Consolidate flow" aside in the chunk bar and the v1 framing of the Dormant
  section. Outsiders pulling the skill have no v1.
- **Install step (when this replaces `pm`):** retire/disable the installed v1 `pm` skill in
  the same act (router collision otherwise — both claim "prioritize the backlog"), and add a
  routing-graph node for `context-to-board-pm` (run skill-graph).
- **Deliberate deviations, confirmed:** two HARD-GATEs (distinct irreversibles: hub-truth
  contamination vs. unchecked plans); non-gerund name (driver-mandated).
- **Calibration:** sizing anchors pending first real v2 workflow runs (see
  `references/sizing.md`).
